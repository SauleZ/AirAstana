import {Bound} from '../bound/bound';
import {CostContainer} from '../cost/cost-container';

/**
 * Detail of the offer corresponding to the id provided in input
 */
export class Offer {

    public bounds: Bound[];
    public costContainer: CostContainer

    constructor() {}

}
