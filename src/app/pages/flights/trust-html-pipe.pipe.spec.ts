import { TrustHtmlPipePipe } from './trust-html-pipe.pipe';

describe('TrustHtmlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TrustHtmlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
